import { Box } from "@mui/material";
import AllTarotCards from "../component/allTarotCards/AllTarotCards";

const Home = () => {
  return (
    <Box p={10}>
      <AllTarotCards />
    </Box>
  );
};

export default Home;
